programa {
  funcao inicio() {
    //a) SE SIMPLES - porque só verificar se a temperatura está acima de 33C
    //b) SE/ SENÃO (composta) - porque existem duas possibilidades: ganhou cupom ou não ganhou
    //c) SE SIMPLES - Porque só verificar se o estoque está zerado
    //d) SE/SENAO (composta) - porque pode liberar o acesso ou imformar que a senha esta incorreta
  }
}
